# Cloudflare Pages — pasta de saída do build (npm run build:web → dist).
# No painel: Settings → Builds → Build command = npm run build:web
#             Build output directory = dist  (ou deixe vazio se wrangler.toml for lido)
# Ver DEPLOY_CLOUDFLARE.md

name = "app-igreja"
compatibility_date = "2026-06-22"
pages_build_output_dir = "dist"
