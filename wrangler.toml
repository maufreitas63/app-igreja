# Cloudflare Pages — pasta de saída do build (npm run build:web → dist).
# O comando de build continua no painel: Settings → Builds → Build command = npm run build:web
# Ver DEPLOY_CLOUDFLARE.md

name = "app-igreja"
compatibility_date = "2026-06-22"
pages_build_output_dir = "dist"
