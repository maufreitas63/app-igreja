# Supabase local / deploy reference.
# Deploy: supabase functions deploy ai-chat --project-ref bldbrsuiwctoaxzcrjoc

[functions.ai-chat]
verify_jwt = false
# Consultas pontuais + tool calling; o cliente espera até 180s.
